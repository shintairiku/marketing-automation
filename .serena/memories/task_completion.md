Before finishing a task: ensure Supabase migrations (if any) have been run via `supabase db push`/`bun run migration:up`, regenerate frontend Supabase types, run backend Ruff checks if touched Python, execute `bun run lint` (and tests if/when present) for frontend, and verify FastAPI/Next servers still start locally (`uv run uvicorn ...`, `bun run dev`). Update documentation or environment examples if you added new env vars or commands.